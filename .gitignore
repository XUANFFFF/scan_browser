# 构建产物
dist/
_build_env/
.buildenv/
build/
*.spec
# 例外：macOS 打包分发包 for-mac-build/ 内的 spec 是交付物，必须入库
!for-mac-build/*.spec

# Python
__pycache__/
*.pyc
*.pyo

# 本地配置文件（含实际 IP，不提交）
config.json

# 本地标记文件
.deps_installed
